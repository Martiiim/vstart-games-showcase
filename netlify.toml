[build]
  command = "npm run build"
  publish = "dist/client"

[functions]
  directory = "dist/server"
  external_node_modules = ["*"]
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
